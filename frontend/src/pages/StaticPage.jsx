import { Link } from "react-router-dom";
export function NotFound() { return <main className="auth-page"><div className="auth-card"><p className="public-brand"><img src="/wcase-logo.png" alt="WCase"/></p><h1>Page not found</h1><p>The page you requested does not exist.</p><Link className="btn-primary" to="/">Go Home</Link></div></main>; }
export function LegalPage({ type }) { const privacy = type === "privacy"; return <main className="legal-page"><Link to="/" className="public-brand" aria-label="WCase home"><img src="/wcase-logo.png" alt="WCase"/></Link><h1>{privacy ? "Privacy Policy" : "Terms of Service"}</h1><p className="legal-notice">Draft placeholder — this document requires review by qualified legal counsel before production use.</p><h2>{privacy ? "Information and content" : "Using WCase"}</h2><p>{privacy ? "WCase stores account information, portfolio content, uploaded media, and visitor enquiries needed to provide the service. Deployment owners must define retention, security, and lawful processing practices." : "Creators are responsible for content they upload and must have permission to publish it. Visitors must not misuse, disrupt, or attempt unauthorized access to the service."}</p><h2>Contact</h2><p>Replace this section with the production operator’s contact details before launch.</p></main>; }
